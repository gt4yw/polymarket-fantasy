# Database connection string
# For local development, use a PostgreSQL connection string like:
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name
# 
# On Railway, this is automatically provided when you add a PostgreSQL service
DATABASE_URL=postgresql://username:password@localhost:5432/polymarket_fantasy

# Server port (optional, defaults to 3000)
# Railway automatically sets PORT, but you can override it locally
PORT=3000

# Node environment (optional)
# Set to 'production' when deploying
NODE_ENV=development
